version: '3.8'

services:
  app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - ./server/uploads:/app/uploads
      - ./server/database.sqlite:/app/database.sqlite
    environment:
      - PORT=3001
      - NODE_ENV=production
    restart: unless-stopped

